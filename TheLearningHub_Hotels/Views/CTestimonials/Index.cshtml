@model IEnumerable<TheLearningHub_Hotels.Models.CTestimonial>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/Adminlayout.cshtml";
}

@* <h1>Index</h1> *@

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TestimonialText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.STATUS)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TestimonialText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hotel.Hotelid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.STATUS)
                </td>
                <td>
                <td>
                       @*  @if (item.STATUS == "Pending")
                        {
                        <form id="form-@item.Testimonialid" method="post" action="/Controller/UpdateTestimonialStatus" style="display:inline;">
                            <input type="hidden" name="id" value="@item.Testimonialid" />
                            <input type="hidden" name="status" id="status-@item.Testimonialid" value="Pending" />

                            <!-- Radio Button for Approve -->
                            <label>
                                <input type="radio" name="statusRadio-@item.Testimonialid" value="Approved" onchange="updateStatus(@item.Testimonialid, 'Approved')" />
                                Approve
                            </label>

                            <!-- Radio Button for Reject -->
                            <label>
                                <input type="radio" name="statusRadio-@item.Testimonialid" value="Rejected" onchange="updateStatus(@item.Testimonialid, 'Rejected')" />
                                Reject
                            </label>
                        </form> *@
                        @* } *@
                </td>


            <td>
                    <a asp-action="UpdateTestimonialStatus"  asp-controller="Admin"asp-route-id="@item.Testimonialid" asp-route-status="Approved">Approved</a>
                    <a asp-action="UpdateTestimonialStatus" asp-controller="Admin" asp-route-id="@item.Testimonialid" asp-route-status="Rejected">Rejected</a>
                    <a asp-action="Edit " asp-route-id="@item.Testimonialid">Edit</a> 
                  <a asp-action="Details" asp-route-id="@item.Testimonialid">Details</a> 
                <a asp-action="Delete" asp-route-id="@item.Testimonialid">Delete</a>
            </td>
        </tr>

}
    </tbody>
</table>
